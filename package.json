{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if(!ua.includes('pnpm')){console.error('Use pnpm instead'); process.exit(1)}\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:db": "docker compose up -d postgres",
    "db:push": "cross-env DATABASE_URL=pglite://d:/gia-shawarma-manager-self-host/.data/gia-shawarma pnpm --filter @workspace/db run push",
    "dev:api": "cross-env DATABASE_URL=pglite://d:/gia-shawarma-manager-self-host/.data/gia-shawarma PORT=5000 pnpm --filter @workspace/api-server run build && cross-env DATABASE_URL=pglite://d:/gia-shawarma-manager-self-host/.data/gia-shawarma PORT=5000 AUTH_SECRET=gia-dev-secret pnpm --filter @workspace/api-server run start",
    "dev:api:pg": "cross-env DATABASE_URL=postgresql://gia_user:gia_local_dev@127.0.0.1:5432/gia_shawarma PORT=5000 AUTH_SECRET=gia-dev-secret pnpm --filter @workspace/api-server run build && cross-env DATABASE_URL=postgresql://gia_user:gia_local_dev@127.0.0.1:5432/gia_shawarma PORT=5000 AUTH_SECRET=gia-dev-secret pnpm --filter @workspace/api-server run start",
    "dev:web": "cross-env PORT=5173 BASE_PATH=/ pnpm --filter @workspace/gia-shawarma-manager run dev"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
